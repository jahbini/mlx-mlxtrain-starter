---

## LICENSE
Choose one and drop the file in (MIT or Apache-2.0 are both fine). If unsure, MIT is shortest.

- MIT → minimal friction for users.
- Apache-2.0 → patent grant included.

---

## What you’ll commit vs. generate

**Committed:**
- `README.md`, `LICENSE`, `.gitignore`
- `environment.yml` (or `requirements.txt`)
- `notebooks/mlx_finetune.ipynb`

**Generated by the notebook (ignored):**
- `data/train.jsonl`, `data/valid.jsonl`
- `experiments.csv`, `artifacts.json`, `generation_policy.json`
- `runs/…` (adapters/fused/quantized)
- `eval_out/…` (reports, final_generations, `repro.sh`)
- `dist/*.tar.gz` (freeze bundles)

---

## Publish checklist (super short)

1) Initialize and push:
```bash
git init
git add .
git commit -m "Initial commit: MLX fine-tuning starter"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
